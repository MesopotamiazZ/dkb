import React, { memo } from 'react';
import './style.less';

const AddExpress = memo(() => {
  return (
    <div className="add-express">
      add express
    </div>
  )
})

export default AddExpress;

