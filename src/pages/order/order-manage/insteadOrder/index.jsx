import React, { memo } from 'react';
import './style.less';

const InsteadOrder = memo(() => {
  return (
    <div className="instead-order">
      instead-order
    </div>
  )
})

export default InsteadOrder;